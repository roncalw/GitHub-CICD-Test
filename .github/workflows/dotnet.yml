name: .NET

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    # runs-on: self-hosted
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v2
      
    - name: Generate build number
      uses: einaregilsson/build-number@v3
      with:
        token: ${{secrets.github_token}}
      
    - name: Build
      run: ddotnet publish --framework net5.0 --configuration Release --self-contained -r win10-x64
  
    - name: Publish
      run: dotnet publish

    - name: Do this
      run: echo "Build number is ${env:BUILD_NUMBER}"
      
